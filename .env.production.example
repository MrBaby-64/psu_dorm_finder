# PSU Dorm Finder - Production Environment Configuration for Render + PostgreSQL
# Copy this file and set these variables in your Render dashboard Environment Variables

APP_NAME="PSU Dorm Finder"
APP_ENV=production
APP_KEY=base64:YourGeneratedKeyHere
APP_DEBUG=false
APP_URL=https://your-app-name.onrender.com

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# PostgreSQL Database Configuration for Render
# IMPORTANT: Get these from your Render PostgreSQL dashboard
DB_CONNECTION=pgsql
DB_HOST=your-postgres-host.oregon-postgres.render.com
DB_PORT=5432
DB_DATABASE=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_secure_password
DB_SSLMODE=require

# Session & Cache (use database for production reliability)
SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_DRIVER=database
QUEUE_CONNECTION=database

# File Storage (use public disk for Render)
FILESYSTEM_DISK=public

# Mail Configuration (use a service like SendGrid, Mailgun, or Gmail)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@psu-dorm-finder.com
MAIL_FROM_NAME="${APP_NAME}"

# Broadcasting
BROADCAST_DRIVER=log

# reCAPTCHA (get from https://www.google.com/recaptcha/)
NOCAPTCHA_SITEKEY=your-recaptcha-site-key
NOCAPTCHA_SECRET=your-recaptcha-secret

# AWS S3 (Optional - for file storage if needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# IMPORTANT NOTES FOR RENDER DEPLOYMENT:
# 1. Set all these as Environment Variables in Render dashboard
# 2. Generate APP_KEY with: php artisan key:generate --show
# 3. Database credentials come from your Render PostgreSQL instance
# 4. Set APP_DEBUG=false for production (security!)
# 5. Use DATABASE for session/cache (file-based won't work on Render)
# 6. DB_SSLMODE=require is mandatory for Render PostgreSQL